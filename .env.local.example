# If using Netlify Supabase extension, it provides these automatically:
# SUPABASE_DATABASE_URL (auto-provided by Netlify)
# SUPABASE_ANON_KEY (auto-provided by Netlify)

# For local development or non-Netlify deployments, use these:
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Shared password hash for family authentication
# Generate with: node scripts/generate-password-hash.js your-password
SHARED_PASSWORD_HASH=your-bcrypt-hash-here
